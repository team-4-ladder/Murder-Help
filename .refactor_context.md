# 채팅(Chat) 도메인 리팩토링 컨텍스트

> 생성일: 2026-09-16 | 대상 경로: `domain/chat` (백엔드), `components/chat` (프론트엔드)

---

## 1. 채팅 도메인 핵심 흐름 요약

### 백엔드 핵심 플로우

```
[고객] POST /api/chat/rooms
  └─> ChatFacade.createRoomAndSendGreeting(memberId)
        ├─ ChatRoomService.createRoom()         → DB 저장, Redis 이벤트 발행
        └─ ChatMessageService.sendBotWelcomeMessage() → BOT 메시지 저장 + Redis Pub/Sub

[고객/관리자] STOMP /pub/chat.send
  └─> ChatStompController.sendMessage()
        └─ ChatMessageService.sendMessage()
              ├─ 권한 검증 (ChatRoomStatus.canSendMessage)
              ├─ 관리자 최초 메시지 → room.assignAdmin() [WAITING → IN_PROGRESS]
              ├─ 메시지 저장 (ChatMessageRepository)
              ├─ Redis Hash "chat_last_messages" 업데이트 (updateLastMessageInRedis)
              ├─ Spring Event → ChatRedisEventListener → ChatRedisPublisher (Pub/Sub)
              └─ BOT_MODE + 고객 → BotCommandDispatcher.execute()
                    └─ BotCommand.CONNECT_AGENT → room.changeToWaiting() + 시스템메시지
                       BotCommand.RECOMMEND_WEAPON / CHECK_ORDER → 봇 응답 메시지

[고객/관리자] PATCH /api/chat/rooms/{roomId}/close
  └─> ChatFacade.closeRoom()
        ├─ ChatRoomService.validateRoomAccess() + closeRoom()  [→ COMPLETED]
        └─ ChatMessageService.sendCloseSystemMessage()
              └─ Redis Hash "chat_last_messages" 해당 방 삭제 + 이벤트 발행

[앱 시작] ApplicationReadyEvent
  └─> ChatCacheRecoveryService.restoreOnStartup()
        └─ 활성 채팅방의 마지막 메시지를 DB→Redis 복구
```

### 상태 전이 (ChatRoomStatus)

```
BOT_MODE ──[고객: CONNECT_AGENT 선택]──> WAITING
WAITING  ──[관리자: 첫 메시지 발송]────> IN_PROGRESS
BOT_MODE / WAITING / IN_PROGRESS ──[close]──> COMPLETED
```

### 프론트엔드 ↔ 백엔드 연동 흐름

```
[고객 화면]
  FloatingChatWidget → ChatModal → ChatRoomView
    └─ useChatRoom(roomId)
          ├─ REST GET  /api/chat/rooms/{roomId}          → 방 상태 초기 조회
          ├─ REST GET  /api/chat/rooms/{roomId}/messages → 과거 메시지 페이징
          ├─ STOMP SUB /sub/chat/room/{roomId}           → 실시간 신규 메시지 수신
          ├─ STOMP SUB /sub/chat/rooms/updates           → 방 상태 변경 실시간 반영
          └─ STOMP PUB /pub/chat.send                    → 메시지 발송

[관리자 화면]
  AdminChatDashboard(adminId)
    ├─ REST GET /api/chat/rooms?status=...              → 방 목록(탭별 필터)
    ├─ REST GET /api/chat/rooms?status=WAITING&size=1   → 대기/진행 중 카운트
    ├─ STOMP SUB /sub/chat/rooms/updates                → 방 목록 실시간 갱신
    └─ ChatRoomView (선택된 방 표시)

[Redis Pub/Sub 구독 (백엔드)]
  ChatRedisSubscriber → SimpMessagingTemplate
    ├─ "chat-room:{roomId}"    → /sub/chat/room/{roomId}    (메시지)
    └─ "chat-room:updates"     → /sub/chat/rooms/updates     (방 상태)
```

---

## 2. 주요 파일 목록 및 의존성

### 백엔드

| 레이어 | 파일 | 핵심 책임 |
|--------|------|-----------|
| Entity | `ChatRoom.java` | 방 상태 전이 메서드, 도메인 로직 포함 |
| Entity | `ChatMessage.java` | 메시지 저장 엔티티 |
| Entity | `ChatRoomStatus.java` | 상태별 행위 정의 (Strategy 패턴 적용 enum) |
| Controller | `ChatRestController.java` | REST API 엔드포인트 (4개 서비스 주입) |
| Controller | `ChatStompController.java` | WebSocket 메시지 수신 |
| Facade | `ChatFacade.java` | 방 생성+봇 인사, 방 종료+시스템메시지 오케스트레이션 |
| Service | `ChatRoomService.java` | 방 CRUD, 접근 권한 검증, Redis 캐시 관리 |
| Service | `ChatMessageService.java` | 메시지 저장/조회, 봇/시스템 메시지 발송, Redis 업데이트 |
| Service | `ChatCacheRecoveryService.java` | 앱 시작 시 Redis 캐시 복구, 관리자 수동 복구 |
| Redis | `ChatRedisPublisher.java` | Redis Pub/Sub 발행 (메시지, 방 상태) |
| Redis | `ChatRedisSubscriber.java` | Redis 채널 구독 → STOMP 브로드캐스트 |
| Event | `ChatRedisEventListener.java` | Spring 트랜잭션 이벤트 → Redis Pub 트리거 |
| Bot | `BotCommandDispatcher.java` | 봇 명령어 파싱 및 핸들러 라우팅 |
| Repository | `ChatRoomRepositoryImpl.java` | QueryDSL 동적 쿼리 (필터/페이징) |

### 프론트엔드

| 파일 | 핵심 책임 |
|------|-----------|
| `chat.types.ts` | ChatRoom/ChatMessage 공통 타입 정의 |
| `useChatRoom.ts` | STOMP 연결, 메시지 로딩, 스크롤 제어 통합 커스텀 훅 |
| `ChatRoomView.tsx` | 고객·관리자 공용 채팅 뷰 (메시지 목록 + 입력폼) |
| `ChatMessageBubble.tsx` | 메시지 타입별 렌더링 (TEXT / SYSTEM / BUTTON) |
| `AdminChatDashboard.tsx` | 관리자 전용 방 목록 + 실시간 갱신 대시보드 |
| `ChatRoomList.tsx` | 고객용 채팅방 목록 |
| `ChatModal.tsx` / `FloatingChatWidget.tsx` | 고객용 채팅 진입 UI |

---

## 3. 발견된 개선/리팩토링 포인트

### 🔴 HIGH - 설계 구조 문제

#### P1. `ChatRestController` — 4개 서비스 직접 주입 (Facade 미활용)
- `ChatRoomService`, `ChatMessageService`, `ChatFacade`, `ChatCacheRecoveryService` 모두 주입
- `ChatFacade`가 있음에도 컨트롤러가 개별 서비스를 직접 사용하는 혼재 패턴
- **개선**: 모든 복합 오케스트레이션을 `ChatFacade`로 위임, 컨트롤러는 `ChatFacade`만 의존

#### P2. `ChatMessageService` — Redis 직접 조작 책임 중복
- `ChatMessageService`와 `ChatRoomService` 양쪽에서 `StringRedisTemplate`을 직접 사용
- `updateLastMessageInRedis()` 메서드가 서비스 계층에 분산됨
- **개선**: Redis 캐시 조작을 `ChatRedisPublisher` 또는 별도 `ChatCacheService`로 단일화

#### P3. `ChatFacade.closeRoom()` — DB 이중 조회
- `getRoomEntity(roomId)` 호출 후 → `validateRoomAccess(room, memberId)` 내부에서 `getMemberById()` 호출
- 이후 `chatRoomService.closeRoom(roomId)` 에서 `getRoomEntity(roomId)` **재조회**
- **개선**: `closeRoom(room)` 오버로드 또는 엔티티를 직접 전달하도록 리팩토링

### 🟡 MEDIUM - 코드 품질 문제

#### P4. `ChatStompController` — 보안 처리 로직 컨트롤러 내 인라인
- 세션에서 `memberId` 추출 → `ChatMessageSendRequest` 재생성 로직이 컨트롤러에 위치
- **개선**: `StompPrincipalArgumentResolver` 또는 별도 인터셉터로 분리 고려

#### P5. `BotCommandDispatcher` — 외부 도메인 서비스 직접 의존
- `OrderService`, `ProductService`, `ProductRankingService` 등 타 도메인 서비스를 직접 주입
- 봇 응답 로직이 비대해질 수 있음 (현재 117줄)
- **개선**: `BotCommandHandler` 인터페이스로 각 핸들러 클래스 분리 (Command 패턴 완성)

#### P6. `chat.types.ts` — `BOT_MODE` 상태 누락
- `ChatRoomStatus` 타입이 `"WAITING" | "IN_PROGRESS" | "COMPLETED"` 3가지만 정의
- 백엔드 `ChatRoomStatus`에는 `BOT_MODE`가 존재하지만 프론트 타입에 미포함
- `useChatRoom.ts`에서 `useState<string>("BOT_MODE")`로 임시 처리 (타입 안전성 없음)
- **개선**: `"BOT_MODE"` 추가 및 `useState<ChatRoomStatus>("BOT_MODE")` 로 변경

#### P7. `AdminChatDashboard` — `size=100` 하드코딩 및 페이징 미구현
- `fetchRooms()`에서 `size=100` 하드코딩으로 서버 사이드 페이징 무력화
- 채팅방이 100개 초과 시 데이터 누락 위험
- **개선**: 가상 스크롤 또는 서버 사이드 페이징 구현

#### P8. `useChatRoom.ts` — STOMP `[CLOSED]` 문자열 하드코딩 판별
- `newMsg.content.includes("[CLOSED]")`로 종료 여부를 판별
- 백엔드 문자열 변경 시 프론트 연동 깨짐
- **개선**: `messageType === "SYSTEM"` + 방 상태 이벤트(`/sub/chat/rooms/updates`)로만 판별

### 🟢 LOW - 개선 사항

#### P9. `ChatCacheRecoveryService` — 단일 책임 원칙 위반 가능성
- 앱 시작 이벤트 리스너 + 관리자 수동 복구 API 양쪽에서 호출하는 `restore()` 공유
- 현재는 문제없으나, 추후 복구 전략 분기 시 복잡도 증가 우려

#### P10. `ChatRoomResponse` — `from()` 오버로드 방식
- `from(chatRoom)` → `from(chatRoom, null)` 위임 방식은 가독성 양호
- 그러나 관리자 정보(admin) 미포함 → 관리자 배정 현황 조회 시 추가 쿼리 발생 가능

---

## 4. 리팩토링 우선순위 요약

| 순위 | 포인트 | 영향도 | 난이도 |
|------|--------|--------|--------|
| 1 | P1. Controller Facade 미활용 | 높음 | 낮음 |
| 2 | P6. 프론트 BOT_MODE 타입 누락 | 중간 | 낮음 |
| 3 | P2. Redis 직접 조작 분산 | 높음 | 중간 |
| 4 | P3. Facade 이중 DB 조회 | 중간 | 낮음 |
| 5 | P8. STOMP [CLOSED] 하드코딩 | 중간 | 낮음 |
| 6 | P7. Admin 페이징 하드코딩 | 중간 | 중간 |
| 7 | P5. BotCommandDispatcher 비대화 | 낮음 | 높음 |
| 8 | P4. STOMP 컨트롤러 보안 로직 | 낮음 | 높음 |

---

## 5. 성능 최적화 및 웹소켓 라이프사이클 조사 (2026-09-16 추가)

> 코드 수정 없이 조사만 수행한 결과입니다. 구현은 이 문서에 대한 승인 이후 진행합니다.

### 5-1. 메시지 목록 커서 기반 페이징 도입

**As-Is**
- `ChatMessageRepositoryImpl.findMessagesByRoomId(Long roomId, Pageable pageable)`가 QueryDSL로 `offset(pageable.getOffset())` + `limit(pageable.getPageSize())`를 쓰고, 별도 `count()` 쿼리(`countQuery`)를 `PageableExecutionUtils.getPage(...)`에 넘겨 `totalElements`/`last` 등을 계산한다.
- 정렬 기준은 `chatMessage.createdAt.desc()` 단독(2차 정렬 키 없음 — 동시성 점검 때 지적된 "동률 시 정렬 불안정" 문제와 동일 근본 원인).
- 프론트(`useChatRoom.ts`)는 `page`/`size` 파라미터로 `GET /api/chat/rooms/{roomId}/messages`를 호출하고, 응답을 `PageResponse<ChatMessageResponse>`(content/empty/first/last/number/numberOfElements/totalElements)로 받아 `isLast`/`page` state를 관리한다.
- 한계: 오프셋 방식은 대화가 오래 쌓인 방일수록 `OFFSET N`이 커져 스캔 비용이 늘고, 무한 스크롤 UX에서는 사실상 안 쓰이는 `totalElements`를 위해 매 페이지 로드마다 `count()` 쿼리 비용을 계속 지불한다.

**To-Be**
- `WHERE chatMessage.chatRoom.id = :roomId AND (:lastMessageId IS NULL OR chatMessage.id < :lastMessageId) ORDER BY chatMessage.id DESC LIMIT :size + 1` 형태로 전환.
  - `id`를 정렬/커서 기준으로 삼아 `createdAt` 동률 문제도 자연히 해소(오토인크리먼트라 정렬이 항상 결정적).
  - `size + 1`건을 조회해 초과분이 있으면 `hasNext = true`, `size`건만 잘라 content로 사용. 마지막 원소의 `id`가 `nextCursorId`.
  - count 쿼리 자체가 불필요해짐(무한 스크롤 UX와 정확히 부합).
- 신규 메서드안: `List<ChatMessage> findMessagesByCursor(Long roomId, Long lastMessageId, int limit)` — `ChatMessageRepositoryCustom`에 추가, `ChatMessageRepositoryImpl`에 QueryDSL로 구현.
- 신규 응답 규격 제안: `CursorPageResponse<T>(List<T> content, boolean hasNext, Long nextCursorId)` record를 `domain/chat/dto`에 추가.

**영향도 및 하위 호환 대책**
- `ChatRestController.getMessageHistory`는 현재 `Pageable pageable`을 받는데, 커서 방식은 `@RequestParam Long lastMessageId, @RequestParam(defaultValue="20") int size`로 파라미터 형태 자체가 바뀌는 **API 계약 변경**이라 프론트(`useChatRoom.ts`의 `loadMoreMessages`, `chat.types.ts`의 `PageResponse` 사용부)도 함께 바꿔야 한다.
- 방 목록 조회(`getMyRooms`/`getAllRooms`, `Page<ChatRoomResponse>`)는 대상이 아니며 그대로 둔다 — 이번 전환 범위는 메시지 조회 한 곳(`ChatMessageService.getMessageHistory` → `ChatFacade.getMessageHistory` → `ChatRestController.getMessageHistory`, 3개 계층)에 한정.
- 프론트 `useChatRoom.ts`는 `page`(숫자) state → `lastMessageId`(커서) state로, `isLast` → `hasNext`(의미가 반전됨)로 바꿔야 한다.

### 5-2. `ChatMessageRepositoryImpl` N+1 방지 (sender fetch join)

- `ChatMessage.sender`는 `@ManyToOne(fetch = FetchType.LAZY) @JoinColumn(name="member_id", nullable=false)`.
- `findMessagesByRoomId`가 이 연관관계를 fetch join 없이 조회하고, 이후 `ChatMessageResponse.from(chatMessage)`가 `chatMessage.getSender().getId()/getEmail()/getName()/getGrade()/getProfileImageUrl()`을 호출하는 시점에 지연 로딩이 트리거된다.
- 실질 영향: `getMessageHistory`가 읽기 전용 트랜잭션 안에서 실행되므로 세션은 열려있어 `LazyInitializationException` 위험은 없고 순수 성능 문제다. 다만 페이지 내 메시지 발신자가 바뀔 때마다(고객/봇/관리자가 번갈아 보낸 대화) 추가 `SELECT * FROM members WHERE id=?`가 발신자 수만큼 발생한다.
- 개선안: `.selectFrom(chatMessage).join(chatMessage.sender).fetchJoin().where(...)`로 변경. `sender`가 `nullable=false`이므로 INNER `join().fetchJoin()`이 안전(이미 `ChatRoomRepository.findByIdWithCustomer`에 적용한 것과 동일 패턴).
- 5-1의 신규 커서 메서드(`findMessagesByCursor`)에도 처음부터 이 fetch join을 포함해서 구현하면 되므로 별도 추가 작업이 아니라 같은 커밋에서 함께 처리.

### 5-3. 웹소켓 세션 라이프사이클 — 입장/퇴장 시스템 메시지

**현재 구조 점검**
- `StompAuthInterceptor`(`ChannelInterceptor`)는 `StompCommand.CONNECT` 시점만 가로채 JWT를 검증하고 `memberId`를 세션 attribute(`accessor.getSessionAttributes().put("memberId", ...)`)에 저장한다. SUBSCRIBE/DISCONNECT는 전혀 다루지 않는다.
- `ChatStompController`는 `/chat.send` 메시지 처리만 있고, 구독/구독해제/연결종료에 반응하는 핸들러가 프로젝트 전체에 하나도 없다(`SessionSubscribeEvent`/`SessionUnsubscribeEvent`/`SessionDisconnectEvent` 리스너 전무 — grep으로 확인).
- 프론트는 `/sub/chat/room/{roomId}` 형태로 특정 방 채널을 구독한다(`useChatRoom.ts`).
- **중요 — 이번 세션에서 구축한 공유 STOMP 커넥션(`chatSocket.ts`)과의 상호작용**: 관리자 대시보드가 방을 전환해도 소켓 연결 자체는 끊기지 않고(`/sub/chat/rooms/updates` 구독이 계속 유지되는 한) `/sub/chat/room/{roomId}` 채널만 구독 해제 후 새 방을 구독한다. 즉 **"방을 나간다"는 이벤트가 소켓 연결 종료(DISCONNECT)와 반드시 일치하지 않는다.** DISCONNECT만 감지하면, 관리자가 방 A → 방 B로 전환할 때 방 A에 대한 LEAVE가 영원히 발생하지 않는 허점이 생긴다.

**설계안**
- `roomId`는 SUBSCRIBE 프레임의 destination 헤더(`/sub/chat/room/{roomId}`)를 파싱해서 얻고, `memberId`는 CONNECT 때 이미 세션 attribute에 있는 값을 재사용한다.
- 신규 컴포넌트(가칭 `ChatSessionEventListener`, `@Component`)에서:
  - `@EventListener(SessionSubscribeEvent.class)` → destination이 `/sub/chat/room/{roomId}` 패턴이면 roomId를 파싱해 세션 attribute(구독 ID 기준)에 저장하고, `ChatMessageType.ENTER` 시스템 메시지를 생성한다.
  - `@EventListener(SessionUnsubscribeEvent.class)` **와** `@EventListener(SessionDisconnectEvent.class)` 둘 다에서 → 해당 구독/세션에 연결된 roomId·memberId를 조회해 `ChatMessageType.LEAVE` 시스템 메시지를 생성한다. (Unsubscribe만으로는 "탭을 그냥 닫아버린 경우"를 못 잡고, Disconnect만으로는 "방만 전환한 경우"를 못 잡으므로 **두 이벤트 다** 필요하다. 단, 같은 세션에서 두 번 LEAVE가 중복 발생하지 않도록 구독 ID 기준으로 한 번만 처리하는 가드가 필요하다.)
  - 구독 ID(`subscriptionId`) ↔ (roomId, memberId) 매핑을 세션 attribute 맵에 함께 저장해, UNSUBSCRIBE/DISCONNECT 시점에 destination 정보 없이도 역추적 가능하게 한다.
- `ChatMessageType`에 `ENTER`, `LEAVE`를 추가한다(현재 `TEXT`, `SYSTEM`, `BUTTON` 3종만 존재). 프론트 `chat.types.ts`의 `ChatMessageType` 유니온과 `ChatMessageBubble.tsx`의 렌더 분기(`m.messageType === "SYSTEM"`)에도 이 두 값을 SYSTEM과 동일한 pill UI로 처리하도록 조건 추가가 필요하다(별도 스타일은 추후 논의).
- 메시지 생성 경로: 기존 `ChatMessageService.sendSystemMessage(ChatRoom room, String content)`가 `ChatMessageType.SYSTEM`을 하드코딩하고 있어, `ChatMessageType`을 파라미터로 받도록 일반화하거나 `sendEnterMessage`/`sendLeaveMessage` 전용 메서드를 추가하는 두 방향 중 택1. 저장 → Redis 발행(`ChatMessageCreatedEvent` → `ChatRedisEventListener` → `ChatRedisPublisher.publish`)까지는 기존 파이프라인을 그대로 재사용 가능하다(신규 배선 불필요).
- 발신자(sender)는 기존 SYSTEM 메시지와 동일하게 `memberService.getSystemBotMember()`(시스템 봇 계정)로 유지하고, 문구에 실제 입장/퇴장한 회원 이름을 포함시키는 방식을 제안한다(예: "OOO님이 입장했습니다").

**미해결 설계 이슈 (구현 전 확인 필요)**
- 재연결(네트워크 순단 후 자동 재연결)마다 SUBSCRIBE가 재발생하는데, 그때마다 ENTER 메시지가 쌓이면 스팸이 된다 — "최초 입장"과 "재연결"을 구분할 지표가 현재 없다. 짧은 시간 내 동일 (roomId, memberId) 재구독은 무시하는 디바운스가 필요할 수 있다.
- 관리자가 대시보드에서 여러 방을 순회할 때마다 ENTER/LEAVE가 계속 발생하는 게 실제로 바람직한지(고객 쪽에는 "상담사가 들어왔다 나갔다"가 반복 노출될 수 있음) 정책 확인이 필요하다.

### 5-4. 구현 단계 요약 (승인 후 진행 순서 제안)

1. `ChatMessageType`에 `ENTER`/`LEAVE` 추가 (백엔드) + `chat.types.ts` 유니온 반영 (프론트)
2. `ChatMessageRepositoryCustom`/`ChatMessageRepositoryImpl`에 커서 기반 `findMessagesByCursor` 추가 (sender fetch join 포함), 기존 `findMessagesByRoomId`는 유지할지 교체할지 결정
3. `domain/chat/dto`에 `CursorPageResponse<T>` 신설
4. `ChatMessageService.getMessageHistory` / `ChatFacade.getMessageHistory` / `ChatRestController.getMessageHistory` 시그니처를 커서 방식으로 전환
5. 프론트 `useChatRoom.ts`의 `loadMoreMessages`/`chat.types.ts`를 커서 응답 규격에 맞게 수정
6. `ChatSessionEventListener` 신설 (SUBSCRIBE/UNSUBSCRIBE/DISCONNECT 처리) + `ChatMessageService`에 ENTER/LEAVE 발송 메서드 추가
7. `ChatMessageBubble.tsx`에 ENTER/LEAVE 렌더 분기 추가
